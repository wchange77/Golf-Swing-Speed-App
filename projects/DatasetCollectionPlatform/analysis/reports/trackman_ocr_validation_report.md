# TrackMan OCR 验证汇总报告

生成时间: 2026-05-12 09:32:28 +08:00

## 结论

本次 TrackMan 导入与 OCR 验证通过。`RapidOCR` 已作为当前批次的最佳可用引擎完成整包处理，输出数据完整，时间对齐结果稳定，可用于后续人工复核和下游入库。

## 依据

| 项目 | 结果 |
|---|---:|
| 输入照片数 | 216 |
| 生成 OCR 文件数 | 216 |
| OCR 引擎 | `rapidocr_onnxruntime` |
| OCR 状态 `ok` | 216 |
| 匹配样本数 | 211 |
| 未匹配样本数 | 18 |
| 未匹配照片数 | 5 |
| 平均时间偏差 | 5.636161 秒 |
| 最大时间偏差 | 28.75 秒 |

### 额外抽样验证

- 216 个 `.ocr.json` 文件全部使用 `rapidocr_onnxruntime`
- 关键界面词命中稳定，例如 `数据`、`击球列表`、`分享`、`设定`、`CURVE`、`CARRY`
- 单图对比结果显示：
  - `RapidOCR`：约 2.32 秒，中文与数值混排识别最好
  - `PaddleOCR`：约 14.3 秒，英文指标更规整，但中文覆盖较弱
  - `Tesseract`：噪声明显，已不作为首选

## 风险

1. 当前结果仍有 18 个样本未匹配，说明采集时间窗口或对齐阈值还需要结合真实业务继续校准。
2. `RapidOCR` 对部分英文指标存在轻微截断或拼写偏差，例如 `LAUNCHANGLE`、`CLUB SPEED` 可能被压缩成相邻字符形式。
3. 这次验证基于现有导出包，尚未覆盖新的 TrackMan 设备版本或更复杂光照条件。

## 下一步

1. 对 5 张未匹配照片做人工复核，确认是否属于时间戳偏移或文件命名异常。
2. 按同一流程补跑一批真实采集数据，观察 `matchedShots` 和 `averageTimeDeltaSeconds` 是否稳定。
3. 如需更高英文指标精度，可对少量关键截图单独尝试 `PaddleOCR` 作为补充通道。

## 验收标准

- 216/216 张照片都生成 `.ocr.json`
- OCR 状态全部为 `ok`
- `index.json` 与 `review.json` 成功生成
- 匹配结果保持 `211/216` 不回退
- 报告中可直接追溯输出目录与核心统计指标
